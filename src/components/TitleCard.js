import styled from 'styled-components';

const TitleCard = styled.h3`
  display: flex;
  align-items: center;
  font-size: 1.8rem;
  margin: 0 0 10px 0;
  font-weight: normal;
`;

export default TitleCard;